name: Validate sentence data

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate sentence files
        shell: pwsh
        run: powershell -ExecutionPolicy Bypass -File scripts/validate-sentences.ps1
